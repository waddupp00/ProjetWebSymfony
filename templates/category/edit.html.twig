{% extends 'base.html.twig' %}

{% block title %}Editer la catégorie{% endblock %}

{% block body %}
    <h1>Editer la catégorie</h1>
    <br/>
    {{ include('category/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
    <a class="btn btn-outline-primary" href="{{ path('category_index') }}">Annuler et retourner à la liste</a>
    {{ include('category/_delete_form.html.twig') }}
{% endblock %}
